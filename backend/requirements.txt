google-genai>=1.20
python-dotenv>=1.0

# PostgreSQL (operative Datenbank, konfiguriert über DATABASE_URL).
# psycopg 3 statt psycopg2: aktiv gepflegt, natives ON CONFLICT/RETURNING-
# Handling, saubere Row-Factories und ein mitgelieferter Connection Pool.
# [binary] liefert die vorkompilierte libpq mit - kein Compiler und keine
# System-Header nötig, weder lokal noch im Render-Build.
psycopg[binary]>=3.1
psycopg-pool>=3.2

pandas>=2.2
openpyxl>=3.1
fastapi>=0.115
uvicorn>=0.30
python-multipart>=0.0.9
PyMuPDF>=1.26

# Auth-Sprint: Verifikation der Supabase-Access-Tokens (backend/auth/).
# Das [crypto]-Extra zieht "cryptography" mit - ohne das könnte PyJWT nur
# HS256 prüfen, nicht die von Supabase standardmässig verwendeten
# asymmetrischen Verfahren (ES256/RS256 über JWKS).
PyJWT[crypto]>=2.8,<3

# Tests (Aufgabe 9)
pytest>=8.0,<9
httpx>=0.27
